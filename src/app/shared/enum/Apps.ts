export enum Apps {
    SMART_OFFICER,
    TRAFFIC_COUNTS,
    ITSM,
    ACCIDENT_RECONSTRUCTION,
    VEMS,
    WANTED_VEHICLES,
    EXPIRED_VEHICLES,
    RCSL,
    DANGEROUS_VIOLATORS,
    SMART_TOWERS,
    PCA,
    DASHBOARDS,
    PLATFORM,
    ONLINE_SHOP
}

/** @deprecated Use Apps.ONLINE_SHOP — kept for localStorage migration from older builds */
export const AppsLegacyPosForm = 14;